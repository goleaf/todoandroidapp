name: todo_server
description: Backend server for Universal TODO App
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.6
  postgres: ^3.0.0
  bcrypt: ^5.1.1
  mailer: ^6.0.2
  dotenv: ^4.1.0

dev_dependencies:
  test: ^1.24.0

